import { View, Text } from 'react-native';

// Placeholder — real implementation lands in a later task.
export function RegistryListScreen() {
  return (
    <View>
      <Text>Registry</Text>
    </View>
  );
}
